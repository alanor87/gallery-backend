const auth = require("./auth");
const public = require("./public");
const users = require("./users");
const images = require("./images");
const interface = require("./interface");

module.exports = { auth, public, users, images, interface };
